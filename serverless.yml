service: my-express-application

provider:
  name: aws
  runtime: nodejs6.10
  stage: dev
  region: eu-west-1


function:
  app:
    handler: index.handler
    events:
     - http: ANY /
     - http: 'ANY {proxy+}'
     